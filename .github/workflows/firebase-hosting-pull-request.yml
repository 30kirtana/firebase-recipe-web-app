# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    env:
      REACT_APP_API_KEY: "${{AIzaSyCUUGz_6ZhW0p-JlneFjOaWRDJ2TUDLTjc}}"
      REACT_APP_AUTH_DOMAIN: "${{fir-recipes-ab2f4.firebaseapp.com}}"
      REACT_APP_PROJECT_ID: "${{fir-recipes-ab2f4}}"
      REACT_APP_STORAGE_BUCKET: "${{fir-recipes-ab2f4.firebasestorage.app}}"
      REACT_APP_MESSAGING_SENDER_ID: "${{277386334199}}"
      REACT_APP_APP_ID: "${{1:277386334199:web:8fd28b0c19881a64b1a8f2}}"
      REACT_APP_MEASUREMENT_ID: "${{G-KTLQ7ZQR0H}}"
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FIR_RECIPES_AB2F4 }}
          projectId: fir-recipes-ab2f4
